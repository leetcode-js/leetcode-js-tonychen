/**
 * @param {number[]} cells
 * @param {number} N
 * @return {number[]}
 */
const prisonAfterNDays = function(cells, N) {
    if (N === 1) {
        return cells
    }
    while (N--) {
        for (let i = )
    }
}
